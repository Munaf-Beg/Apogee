# 🚀 Apogee Events App

A sleek, high-performance mobile dashboard built with **React Native (Expo)** and **TypeScript** to manage and explore events for the Apogee fest.

## 📋 Task Details
- **Option** Mobile Dashboard Development
- **Stack** React Native (Expo) and Typescript.

## ✨ Features
- **Live Search:** Instant filtering of events by name.
- **Category Chips:** Quick-filter events by category (Music, Tech, Dance, Misc).
- **Dynamic Stats:** Automatically calculates the "Most Popular Category" using registration data.
- **Smart Sorting:** Toggle events by registration count to see what's trending.
- **Interactive Cards:** Independent "Save" state for each event card with haptic-style visual feedback.
- **Safe UI:** Optimized for modern devices with Notch/Safe Area handling.

## 📂 Project Structure
- `app/`: Main screens and root layout (Expo Router).
- `src/components/`: Reusable UI units like the `EventCard`.
- `src/data/`: Centralized event data store.
- `src/types/`: TypeScript interfaces (The blueprints of the app).

## 🛠️ Tech Stack
- **Framework:** React Native (Expo)
- **Language:** TypeScript
- **State Management:** React Hooks (`useState`)
